export function ToolsPage() {
  return (
    <section className="mx-auto w-full max-w-6xl px-4 py-16 sm:px-6 lg:py-24">
      <p className="text-sm font-semibold uppercase tracking-[0.18em] text-primary">Document toolkit</p>
      <h1 className="mt-4 max-w-3xl text-4xl font-bold tracking-tight text-foreground sm:text-5xl">
        Everything your documents need, in one lane.
      </h1>
      <p className="mt-5 max-w-2xl text-lg leading-8 text-muted-foreground">
        This route will later house the full concept tool catalogue. The current foundation does not perform real
        document processing or document-upload network requests.
      </p>
    </section>
  );
}
